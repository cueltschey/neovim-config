# Hello

This is my slimmed down neovim configuration. If you want to use it please feel free to run:
```bash
git clone https://github.com/cueltschey/neovim-config ~/.local/config/nvim
nvim
```

Note that you need a newer version of neovim that may not be available on distributions like ubuntu
